using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;
namespace ExtensionMethods.Classes
{
    public static class IReferenceDataSourceExtensions
    {

        //public static IEnumerable<ReferenceDataItem> GetItems(this IReferenceDataSourceExtensions source, string code)
        //{

        //    return source.GetItems().Where(x => x.Code == code);
        //}
    }
}
